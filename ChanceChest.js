import SpecialSpace from './SpecialBlock';

class Chance extends SpecialBlock {
    constructor() {
        super('Chance');
    }

    action(player) {
        super.action(player);
    }
}

export default Chance;
